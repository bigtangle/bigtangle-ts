// This file will contain placeholder definitions for protobuf-generated enums and interfaces.
// In a real project, these would be generated from .proto files using a tool like `protoc`.

export namespace Protos {
    export namespace Wallet {
        export enum EncryptionType {
            UNENCRYPTED = 0,
            AES256_SHA256_HMAC = 1,
            // Add other encryption types as needed
        }
    }
}
